# Workflow Tools

A collection of CLI (command-line interface) tools for building and running workflows.

## Packages

- `verbs-cli` — a CLI tool for running Git workflows.
- `@amirab/prompt` — a minimal CLI prompting tool.
